<%= form_with(model: editorial_list, local: true) do |form| %>
  <% if editorial_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(editorial_list.errors.count, "error") %> prohibited this editorial_list from being saved:</h2>

      <ul>
      <% editorial_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :about %>
    <%= form.text_area :about %>
  </div>

  <div class="field">
    <%= form.label :slug %>
    <%= form.text_field :slug %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
